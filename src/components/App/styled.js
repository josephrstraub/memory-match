import styled from 'styled-components'

export const theme = {
  blue: '#359DED'
}

export const TileContainer = styled.div`
  margin: auto;
  max-width: 440px;
  display: inline-flex;
  flex-wrap: wrap;
`
